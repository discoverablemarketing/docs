---
title: "Quickstart"
description: "Make your first ChatAds API call in under 5 minutes"
---

## Prerequisites

- A ChatAds account ([sign up here](https://app.getchatads.com))
- An API key from your [dashboard](https://app.getchatads.com/settings/api-keys)

## Step 1: Get Your API Key

1. Log into [app.getchatads.com](https://app.getchatads.com)
2. Navigate to **Settings** → **API Keys**
3. Click **Create API Key**
4. Copy your new API key (starts with `cak_`)

<Warning>
  Keep your API key secure. Never expose it in client-side code or public repositories.
</Warning>

## Step 2: Make Your First Request

<CodeGroup>

```bash cURL
curl -X POST 'https://chatads--chatads-api-fastapiserver-serve.modal.run/v1/chatads/messages' \
  -H 'Content-Type: application/json' \
  -H 'x-api-key: YOUR_API_KEY' \
  -d '{
    "message": "What are the best noise-cancelling headphones?"
  }'
```

```typescript TypeScript
import { ChatAdsClient } from '@chat-ads/chatads-sdk';

const client = new ChatAdsClient({ apiKey: 'YOUR_API_KEY' });

const response = await client.analyzeMessage({
  message: "What are the best noise-cancelling headphones?"
});

console.log(response.data);
```

```python Python
from chatads_sdk import ChatAdsClient

with ChatAdsClient(api_key="YOUR_API_KEY") as client:
    response = client.analyze_message(
        message="What are the best noise-cancelling headphones?"
    )
    print(response.data)
```

```javascript Node.js
const url = "https://chatads--chatads-api-fastapiserver-serve.modal.run/v1/chatads/messages";

const response = await fetch(url, {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "x-api-key": "YOUR_API_KEY"
  },
  body: JSON.stringify({
    message: "What are the best noise-cancelling headphones?"
  })
});

const data = await response.json();
console.log(data);
```

```ruby Ruby
require 'net/http'
require 'json'

url = URI("https://chatads--chatads-api-fastapiserver-serve.modal.run/v1/chatads/messages")

http = Net::HTTP.new(url.host, url.port)
http.use_ssl = true

request = Net::HTTP::Post.new(url.path, {
  "Content-Type" => "application/json",
  "x-api-key" => "YOUR_API_KEY"
})

request.body = { message: "What are the best noise-cancelling headphones?" }.to_json

response = http.request(request)
puts JSON.parse(response.body)
```

```php PHP
<?php
$url = "https://chatads--chatads-api-fastapiserver-serve.modal.run/v1/chatads/messages";

$payload = ["message" => "What are the best noise-cancelling headphones?"];

$ch = curl_init($url);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($payload));
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    "Content-Type: application/json",
    "x-api-key: YOUR_API_KEY"
]);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
curl_close($ch);

print_r(json_decode($response, true));
?>
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
)

func main() {
    url := "https://chatads--chatads-api-fastapiserver-serve.modal.run/v1/chatads/messages"
    payload := []byte(`{"message": "What are the best noise-cancelling headphones?"}`)

    req, _ := http.NewRequest("POST", url, bytes.NewBuffer(payload))
    req.Header.Set("Content-Type", "application/json")
    req.Header.Set("x-api-key", "YOUR_API_KEY")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    body, _ := io.ReadAll(resp.Body)
    fmt.Println(string(body))
}
```

```java Java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

public class ChatAdsExample {
    public static void main(String[] args) throws Exception {
        String url = "https://chatads--chatads-api-fastapiserver-serve.modal.run/v1/chatads/messages";
        String payload = "{\"message\": \"What are the best noise-cancelling headphones?\"}";

        HttpClient client = HttpClient.newHttpClient();
        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create(url))
            .header("Content-Type", "application/json")
            .header("x-api-key", "YOUR_API_KEY")
            .POST(HttpRequest.BodyPublishers.ofString(payload))
            .build();

        HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());
    }
}
```

</CodeGroup>

## Step 3: Handle the Response

A successful response looks like this:

```json
{
  "success": true,
  "data": {
    "matched": true,
    "filled": true,
    "ad": {
      "product": "Noise-Cancelling Headphones",
      "link": "https://www.amazon.com/dp/B0XXXXXXXXX?tag=yourtag-20",
      "message": "Block out distractions with premium ANC headphones.",
      "category": "electronics"
    },
    "keyword": "noise-cancelling headphones"
  },
  "meta": {
    "request_id": "d6a8f6f2-3b5a-4f0a-81ab-1b4a4c9dd5ea",
    "country": "US",
    "language": "en",
    "extraction_method": "llm"
  }
}
```

## Response Fields

| Field | Description |
|-------|-------------|
| `success` | Whether the request was processed successfully |
| `data.matched` | Whether a product keyword was found |
| `data.filled` | Whether an affiliate link was resolved |
| `data.ad` | The affiliate link details (if matched) |
| `data.keyword` | The extracted product keyword |
| `meta.request_id` | Unique ID for debugging and support |

## Next Steps

<CardGroup cols={2}>
  <Card
    title="Authentication"
    icon="key"
    href="/guides/authentication"
  >
    Learn about API key management and security.
  </Card>
  <Card
    title="Rate Limits"
    icon="gauge"
    href="/guides/rate-limits"
  >
    Understand usage quotas and limits.
  </Card>
</CardGroup>
